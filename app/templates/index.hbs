<div class="row">
	<div class="col">
		<BsButton @type="primary" @onClick={{action (mut xyz)}}>Generate</BsButton>
	</div>
	<div class="col">
		<BsButton @type="success" @onClick={{action (mut xyz)}}>Encrypt</BsButton>
	</div>
	<div class="col">
		<BsButton @type="danger" @onClick={{action (mut xyz)}}>Decrypt</BsButton>
	</div>
</div>
